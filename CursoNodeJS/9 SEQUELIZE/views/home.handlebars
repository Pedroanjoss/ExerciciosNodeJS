<h1>Usuaríos Cadastrados</h1>
<div class="container">
    {{#each users}}
    <div class="item">
        <p>{{this.name}}</p>
        <p>{{this.ocupation}}</p>
        <div class="actions">
            <a href="/users/{{this.id}}">Ver detalhes</a>
        </div>
    </div>
    {{/each}}
</div>